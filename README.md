Repository prepared for submission.

Structure
- Question folders: place each attempted question in its own folder, e.g. `Question_01/`, `Question_02/`.
- Keep all code on a single branch (main).

How to use
1. Add your code inside `Question_xx/` folders.
2. When ready, provide the GitHub repo URL and I'll add the remote and push.

Commit history
- `main` branch: initial commit contains `.gitignore` and folder structure.
